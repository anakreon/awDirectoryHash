'use strict';
const expect = require('chai').expect;
const index = require('../lib/index.js');

describe('Hasher', () => {    
    it('', () => {
        expect(index.Hasher).to.not.be.undefined;
    });
});
